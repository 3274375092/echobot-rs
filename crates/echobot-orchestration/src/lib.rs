//! `echobot-orchestration` implements the Decision - Roleplay - Agent
//! coordinator: it routes turns to chat or agent, generates persona replies,
//! tracks background jobs, and exposes the per-session `RouteMode` settings.

pub fn placeholder() {}

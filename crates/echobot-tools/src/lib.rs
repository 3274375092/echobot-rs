//! `echobot-tools` hosts the tool framework used by the background agent:
//! a `Tool` trait, a `ToolRegistry` for discovery, and the built-in tools
//! (filesystem, shell, git, web, cron, media, memory search, planning).

pub fn placeholder() {}
